# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'CI Demo' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for CI Demo
  pod 'Fabric'                            # https://docs.fabric.io/apple/changelog.html#fabric
  pod 'Crashlytics'                       # https://docs.fabric.io/apple/changelog.html#crashlytics
  pod 'Firebase/Core'

  target 'CI DemoTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'CI DemoUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
